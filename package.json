{
  "scripts": {
    "start": "npx live-server client --host=localhost --port=5500 --no-browser"
  },
  "devDependencies": {
    "eslint": "^8.30.0",
    "eslint-plugin-functional": "^4.4.1",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "gsap": "^3.11.4"
  }
}
